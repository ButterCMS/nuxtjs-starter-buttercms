{
  "private": true,
  "scripts": {
    "dev": "nuxi dev",
    "build": "nuxi build",
    "start": "npm run build && node .output/server/index.mjs",
    "heroku-postbuild": "npm run build"
  },
  "devDependencies": {
    "nuxt": "^3.15.1"
  },
  "dependencies": {
    "buttercms": "^3.0.0",
    "tiny-slider": "^2.9.4",
    "vue": "^3.2.47"
  },
  "engines": {
    "node": ">=22.0.0"
  }
}
